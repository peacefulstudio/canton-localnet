canton.participants.c-validator-1 {
  ledger-api{
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${AUTH_C_VALIDATOR_1_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${AUTH_C_VALIDATOR_1_VALIDATOR_USER_NAME}
  }
}
