canton.participants.c-validator-1 = ${_participant} {
  storage.config.properties.databaseName = participant-c-validator-1
  monitoring {
    http-health-server.port = 13${CANTON_HTTP_HEALTHCHECK_PORT_SUFFIX}
    grpc-health-server.port= 13${CANTON_GRPC_HEALTHCHECK_PORT_SUFFIX}
  }
  http-ledger-api.port = 13${PARTICIPANT_JSON_API_PORT_SUFFIX}
  admin-api.port = 13${PARTICIPANT_ADMIN_API_PORT_SUFFIX}
  ledger-api.port = 13${PARTICIPANT_LEDGER_API_PORT_SUFFIX}
  parameters.alpha-version-support = yes
}
