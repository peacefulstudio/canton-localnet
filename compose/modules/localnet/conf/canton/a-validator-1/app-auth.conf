canton.participants.a-validator-1 {
  ledger-api {
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${AUTH_A_VALIDATOR_1_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${AUTH_A_VALIDATOR_1_VALIDATOR_USER_NAME}
  }
}
