canton.participants.b-validator-1 {
  ledger-api{
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${AUTH_B_VALIDATOR_1_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${AUTH_B_VALIDATOR_1_VALIDATOR_USER_NAME}
  }
}
