canton.validator-apps.a-validator-1-backend = ${_validator_backend} {
  onboarding.secret = ${?A_VALIDATOR_1_VALIDATOR_ONBOARDING_SECRET}
  storage.config.properties.databaseName = validator-a-validator-1
  admin-api.port = 11${VALIDATOR_ADMIN_API_PORT_SUFFIX}
  participant-client {
    admin-api.port = 11${PARTICIPANT_ADMIN_API_PORT_SUFFIX}
    ledger-api.client-config.port = 11${PARTICIPANT_LEDGER_API_PORT_SUFFIX}
  }
  validator-party-hint = ${A_VALIDATOR_1_PARTY_HINT}

  domains.global.buy-extra-traffic {
    min-topup-interval = ${MIN_TRAFFIC_TOPUP_INTERVAL}
    target-throughput = ${TARGET_TRAFFIC_THROUGHPUT}
  }
}
