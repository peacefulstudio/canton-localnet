canton.remote-participants.c-validator-1 {
  ledger-api {
    address = canton
    port = 13${PARTICIPANT_LEDGER_API_PORT_SUFFIX}
  }
  admin-api {
    address = canton
    port = 13${PARTICIPANT_ADMIN_API_PORT_SUFFIX}
  }
}
